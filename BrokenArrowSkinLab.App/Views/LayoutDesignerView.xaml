<UserControl x:Class="BrokenArrowSkinLab.App.Views.LayoutDesignerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrokenArrowSkinLab.App.Views"
             xmlns:vm="clr-namespace:BrokenArrowSkinLab.App.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.DataContext>
        <vm:LayoutDesignerViewModel />
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Panel - Toolbox -->
        <DockPanel Grid.Row="0" Grid.RowSpan="3" Grid.Column="0">
            <TextBlock DockPanel.Dock="Top" Text="Control Toolbox" FontWeight="Bold" Margin="5"/>
            
            <StackPanel DockPanel.Dock="Top" Margin="5">
                <Button Content="Add Knob" Command="{Binding AddKnobCommand}" Margin="0,0,0,5"/>
                <Button Content="Add Slider (H)" Command="{Binding AddSliderHCommand}" Margin="0,0,0,5"/>
                <Button Content="Add Slider (V)" Command="{Binding AddSliderVCommand}" Margin="0,0,0,5"/>
                <Button Content="Add Button" Command="{Binding AddButtonCommand}" Margin="0,0,0,5"/>
                <Button Content="Add Switch" Command="{Binding AddSwitchCommand}" Margin="0,0,0,5"/>
                <Button Content="Add LED Meter" Command="{Binding AddMeterCommand}" Margin="0,0,0,5"/>
                <Separator Margin="0,10"/>
                <Button Content="Remove Control" Command="{Binding RemoveControlCommand}" Margin="0,0,0,5"/>
                <Button Content="Duplicate" Command="{Binding DuplicateControlCommand}" Margin="0,0,0,5"/>
            </StackPanel>
            
            <TextBlock DockPanel.Dock="Top" Text="Controls" FontWeight="Bold" Margin="5,15,5,5"/>
            <ListBox DockPanel.Dock="Top" 
                     ItemsSource="{Binding Controls}" 
                     SelectedItem="{Binding SelectedControl}"
                     DisplayMemberPath="Name"
                     Margin="5"/>
        </DockPanel>
        
        <GridSplitter Grid.Row="0" Grid.RowSpan="3" Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>
        
        <!-- Center Panel - Canvas -->
        <DockPanel Grid.Row="0" Grid.RowSpan="2" Grid.Column="2">
            <ToolBar DockPanel.Dock="Top">
                <Button Content="🔍+" Command="{Binding ZoomInCommand}" ToolTip="Zoom In"/>
                <Button Content="🔍-" Command="{Binding ZoomOutCommand}" ToolTip="Zoom Out"/>
                <Button Content="🔍" Command="{Binding ZoomFitCommand}" ToolTip="Zoom to Fit"/>
                <Separator/>
                <ToggleButton Content="Grid" IsChecked="{Binding ShowGrid}" ToolTip="Toggle Grid"/>
                <Separator/>
                <TextBlock Text="{Binding ZoomLevel, StringFormat='Zoom: {0:P0}'}" VerticalAlignment="Center" Margin="5,0"/>
            </ToolBar>
            
            <Border BorderBrush="Gray" BorderThickness="1" Background="#F5F5F5" Margin="5">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Canvas Width="{Binding CurrentLayout.Width, FallbackValue=800}" 
                           Height="{Binding CurrentLayout.Height, FallbackValue=600}">
                        <Canvas.LayoutTransform>
                            <ScaleTransform ScaleX="{Binding ZoomLevel}" ScaleY="{Binding ZoomLevel}"/>
                        </Canvas.LayoutTransform>
                        
                        <!-- Grid Background -->
                        <Canvas.Background>
                            <VisualBrush TileMode="Tile" 
                                        Viewport="0,0,10,10" 
                                        ViewportUnits="Absolute"
                                        Viewbox="0,0,10,10"
                                        ViewboxUnits="Absolute"
                                        Opacity="{Binding ShowGrid, Converter={StaticResource BoolToOpacityConverter}}">
                                <VisualBrush.Visual>
                                    <Grid Background="White">
                                        <Path Data="M 0,0 L 10,0 M 0,0 L 0,10" Stroke="LightGray" StrokeThickness="0.5"/>
                                    </Grid>
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Canvas.Background>
                        
                        <!-- Controls -->
                        <ItemsControl ItemsSource="{Binding Controls}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemContainerStyle>
                                <Style>
                                    <Setter Property="Canvas.Left" Value="{Binding Bounds.X}"/>
                                    <Setter Property="Canvas.Top" Value="{Binding Bounds.Y}"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="Black" 
                                           BorderThickness="1" 
                                           Background="LightGray"
                                           Width="{Binding Bounds.Width}"
                                           Height="{Binding Bounds.Height}">
                                        <TextBlock Text="{Binding Name}" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"
                                                  FontSize="10"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Canvas>
                </ScrollViewer>
            </Border>
        </DockPanel>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Grid.Column="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Layout: "/>
                    <TextBlock Text="{Binding CurrentLayout.Width, FallbackValue=800}"/>
                    <TextBlock Text=" x "/>
                    <TextBlock Text="{Binding CurrentLayout.Height, FallbackValue=600}"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:BoolToOpacityConverter x:Key="BoolToOpacityConverter"/>
    </UserControl.Resources>
</UserControl>
