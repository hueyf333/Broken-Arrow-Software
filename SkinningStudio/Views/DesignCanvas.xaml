<UserControl x:Class="SkinningStudio.Views.DesignCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkinningStudio.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <DrawingBrush TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute">
                <DrawingBrush.Drawing>
                    <DrawingGroup>
                        <GeometryDrawing Brush="#1A1A1A">
                            <GeometryDrawing.Geometry>
                                <RectangleGeometry Rect="0,0,20,20"/>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                        <GeometryDrawing Brush="#252525">
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <RectangleGeometry Rect="0,0,1,20"/>
                                    <RectangleGeometry Rect="0,0,20,1"/>
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                        </GeometryDrawing>
                    </DrawingGroup>
                </DrawingBrush.Drawing>
            </DrawingBrush>
        </Grid.Background>

        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Canvas x:Name="MainCanvas" Width="2000" Height="2000">
                <ItemsControl ItemsSource="{Binding Project.RootElements}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="Canvas.Left" Value="{Binding X}"/>
                            <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                            <Setter Property="Canvas.ZIndex" Value="{Binding ZIndex}"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{Binding IsSelected, Converter={StaticResource BoolToSelectionBrushConverter}}"
                                    BorderThickness="{Binding IsSelected, Converter={StaticResource BoolToSelectionThicknessConverter}}"
                                    Width="{Binding Width}"
                                    Height="{Binding Height}"
                                    Background="#2D2D2D"
                                    Visibility="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                                <ContentControl>
                                    <ContentControl.Style>
                                        <Style TargetType="ContentControl">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementType}" Value="Button">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Button Content="{Binding Text}" 
                                                                    HorizontalAlignment="Stretch" 
                                                                    VerticalAlignment="Stretch"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ElementType}" Value="Label">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Label Content="{Binding Text}" 
                                                                   HorizontalAlignment="Stretch" 
                                                                   VerticalAlignment="Stretch"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ElementType}" Value="TextBox">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <TextBox Text="{Binding Text}" 
                                                                     HorizontalAlignment="Stretch" 
                                                                     VerticalAlignment="Stretch"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ElementType}" Value="CheckBox">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <CheckBox Content="{Binding Text}" 
                                                                      HorizontalAlignment="Stretch" 
                                                                      VerticalAlignment="Stretch"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ElementType}" Value="Slider">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Slider HorizontalAlignment="Stretch" 
                                                                    VerticalAlignment="Center"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ElementType}" Value="Panel">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Border Background="#3C3C3C" 
                                                                    BorderBrush="#606060" 
                                                                    BorderThickness="1">
                                                                <TextBlock Text="{Binding Name}" 
                                                                           Margin="5" 
                                                                           VerticalAlignment="Top"/>
                                                            </Border>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ElementType}" Value="Image">
                                                    <Setter Property="Content">
                                                        <Setter.Value>
                                                            <Border Background="#3C3C3C" 
                                                                    BorderBrush="#606060" 
                                                                    BorderThickness="1">
                                                                <TextBlock Text="[Image]" 
                                                                           HorizontalAlignment="Center" 
                                                                           VerticalAlignment="Center"/>
                                                            </Border>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentControl.Style>
                                </ContentControl>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Canvas>
        </ScrollViewer>

        <TextBlock Text="Design Canvas" 
                   Foreground="#606060" 
                   FontSize="48" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   IsHitTestVisible="False"
                   Opacity="0.3"/>
    </Grid>
</UserControl>
